# Project Demo: Model Classification Showcase

This project demonstrates various machine learning models for image classification.

## Overview

* **`project-demo.ipynb`**: Contains the main application demo.
* **`model_classes/`**: Houses the core model implementations:
    * `ConvolutionalNeuralNetwork.py`: Convolutional Neural Network (CNN) model.
    * `FeedForwardNeuralNetwork.py`: Feed-Forward Neural Network model.
    * `MnistClassifierInterface.py`: Abstract interface for MNIST classifiers.
    * `randomForest.py`: Random Forest classifier.
* **`main.py`**: Entry point for running the demo. Loads and executes the models from `model_classes`.


## Dependencies
Ensure you have the following Python libraries installed:
	pip install numpy scikit-learn tensorflow Pillow matplotlib


